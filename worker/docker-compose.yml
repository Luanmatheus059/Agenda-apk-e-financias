version: '3.8'

services:
  financas-worker:
    build: .
    container_name: financas-agente
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./.state.json:/app/.state.json
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
