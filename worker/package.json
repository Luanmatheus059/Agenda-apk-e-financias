{
  "name": "financas-worker",
  "version": "1.0.0",
  "description": "Agente financeiro 24/7 — roda no Oracle Cloud Free, manda push via Telegram",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env src/index.js",
    "dev": "node --watch --env-file=.env src/index.js"
  },
  "dependencies": {
    "node-telegram-bot-api": "^0.66.0",
    "node-cron": "^3.0.3"
  },
  "engines": {
    "node": ">=20"
  }
}
